# Vendored Zinder wallet protocol

The files below `zinder/v1/` are copied from
`crates/zinder-proto/proto/zinder/v1/` in the adjacent Zinder integration
worktree. They are generated into a client-only tonic stub by `build.rs`.

The integration currently tracks Zinder branch `feat/zallet-backend-contract`
based on commit `6871e5e6abeed1e06633ca95aa4baf893d888be7`. Replace the worktree marker in
`COMMIT` with the final Zinder commit before publishing or opening an upstream
pull request.
